{
  "name": "conventional-commit-msg",
  "author": "Łukasz Jerciński",
  "license": "MIT",
  "version": "1.0.0",
  "description": "Zero-config commitlint frontend for conventional commits",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jercik/conventional-commit-msg.git"
  },
  "type": "module",
  "bin": {
    "conventional-commit-msg": "bin/conventional-commit-msg"
  },
  "files": [
    "bin/",
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "prepublishOnly": "pnpm run rebuild",
    "build": "tsc -p tsconfig.app.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "fta": "fta-check",
    "knip": "knip",
    "lint": "eslint",
    "rebuild": "pnpm run clean && pnpm run build",
    "start": "pnpm -s run rebuild && node bin/conventional-commit-msg",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc -b --noEmit"
  },
  "keywords": [],
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">=22.14.0"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^14.0.0",
    "@commitlint/config-conventional": "^20",
    "@commitlint/format": "^20",
    "@commitlint/lint": "^20",
    "@commitlint/load": "^20",
    "@commitlint/read": "^20",
    "@commitlint/types": "^20",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/node": "^24.10.4",
    "@vitest/coverage-v8": "^4.0.15",
    "eslint": "^9.39.2",
    "eslint-config-axpoint": "^1.0.0",
    "fta-check": "^1.5.0",
    "fta-cli": "^3.0.0",
    "knip": "^5.74.0",
    "prettier": "3.7.4",
    "semantic-release": "^25.0.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
