#!/usr/bin/env bash
# Native Git hook - enable with: git config core.hooksPath .githooks
# Also supports CI usage: .githooks/commit-msg --from <sha> --to <sha>
set -euo pipefail

npx -y conventional-commit-msg --verbose "$@"
